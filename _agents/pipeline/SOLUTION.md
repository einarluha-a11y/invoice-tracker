# SOLUTION

PHASE: ARCHITECTURE
ROUND: 1
TASK: TASK-15 Round 2 — Аккаунт-switcher всё ещё виден, убрать полностью

## ПРОБЛЕМА

Исправление не сработало — оба dropdown всё ещё видны в хедере.
Скриншот подтверждает: "Global Technics" (синяя рамка) + "Global Technics OÜ" рядом.

## РАДИКАЛЬНОЕ РЕШЕНИЕ

Найди в коде компонент аккаунт-switcher (скорее всего AccountSwitcher или похожее название).
Убери его из хедера ПОЛНОСТЬЮ — просто удали JSX тег.

Оставить только компания-switcher ("Global Technics OÜ" — он работает корректно).

После этого сделай `npm run build` и убедись что хедер содержит только один dropdown.

## Верификация
- `npm run build` без ошибок
- Открыть приложение — в хедере ОДИН dropdown компании

